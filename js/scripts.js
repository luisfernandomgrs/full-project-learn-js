// Object

const person = {
    name: 'Pedro',
    age: 45,
    weight: 78.5,
    isAdmin: true
}

console.log(person)
console.log(person.name)
console.log(person.age)

console.log(`${person.name} tem ${person.age} anos.`)